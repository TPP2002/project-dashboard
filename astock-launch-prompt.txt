# 【看板整项目派单】此对话由项目管理看板一键启动,负责落地本项目全部已拍板决策

你是被【项目管理看板】自动派来的对话——用户在看板上一键选择了"打包派单本项目全部已拍板未落地的 4 条决策",本对话被启动、cwd 已进项目主仓、这条 prompt 就是完整任务书。你不需要问用户"要做什么"。

**项目**:A股模拟器 (项目 id: `game`,已接入看板)
**共 4 条待落地决策**,按下方顺序执行:

---

## 1. P10 · 不变量集成 · 决策 #d1(用户拍于 2026-07-01)

**问题**:"玩家优先权"和"先来先服务"这两条规则打架时，程序里的自动体检器要不要给玩家单开绿灯？

**用户拍的答案**:

```
严格不豁免（体检器一视同仁）
```

**当时看板给的推荐理由**(参考,以用户答案为准):方案作者认为：这个规则文件是项目里最基础的"宪法"，动它风险和代价都高。而玩家优先权带来的"日志噪音"其实是可以在别的层面消化的（比如日志里加个标记「玩家单」，人工过滤时一眼就能识别）。用小小的噪音，换来核心规则文件的安全，这笔账划算。

---

## 2. P10 · 不变量集成 · 决策 #d2(用户拍于 2026-07-01)

**问题**:新加的 2 个体检器要接在"订单簿"这个核心文件里，这里现在没有依赖体检器模块——首次引入，可以吗？

**用户拍的答案**:

```
引入（方案 A：接进 orderbook.ts）
```

**当时看板给的推荐理由**(参考,以用户答案为准):方案作者认为：matchEngine 已经开了这个口子（也是核心撮合模块，也接了体检器，跑了这么久没出问题），说明「核心模块接体检器」这条路是安全的。为了保持统一风格、方便以后扩展，orderbook.ts 也走同一条路更值得。多的这一条依赖是可控的（有 try/catch 兜底），换来的是代码一致性和未来的省事。

---

## 3. P10 · 不变量集成 · 决策 #d3(用户拍于 2026-07-01)

**问题**:P10 什么时候开工？现在就动，还是等"金融收尾"的 50 天长测跑完再动？

**用户拍的答案**:

```
P14的长测好像都跑完了吧？
```

**当时看板给的推荐理由**(参考,以用户答案为准):方案作者说得直白：「阶段 0 那次就是同时改多件事、异常归因搞不清才踩了坑」。既然踩过坑、总结出规矩了，就别再赌一次。P10 是独立任务，晚一两周没损失；万一并行出问题，代价是 3 天以上的分析时间，划不来。

---

## 4. P15 · 同价优先权降级 · 决策 #d1(用户拍于 2026-07-01)

**问题**:"玩家同价优先权按难度降级"这个任务：维持暂缓，还是搞个不依赖"职业晋升"的临时版顶着？

**用户拍的答案**:

```
维持暂缓，同时编写一套完整的职业晋升系统，编写完成后做对抗审查，且联网校验
还要再编写一套角色身份系统，因为我在考量是否应当设计角色系统，根据不同身份，给予不同的增益减益和开局资金之类的东西，这个写完也要做对抗审查，同时联网搜索过往任何类型游戏的优秀角色身份设计机制和数值之类的东西，进行取长补短，完善设计方案，最后由我定夺
```

**当时看板给的推荐理由**(参考,以用户答案为准):方案作者的推荐是维持暂缓。理由是：临时版有明确的"过期日"——将来一定会被取代。这就是典型的"一次性投入"：花的每一分工作量，都是知道要打水漂的。除非你有非常明确的"必须现在就上线"的理由，否则等一起做更省事。

关键判断点是：你觉得"职业晋升系统"多久内会做？如果 3 个月内会做，就暂缓；如果排期不明或超过半年，可以考虑临时版顶一下。

---

## 你的施工职责(按顺序执行上面每条决策)

**读了再动手**:
1. 项目根 `CLAUDE.md`——本项目的看板同步纪律(pre-commit 硬闸门会拦你不 claim 就 commit)
2. skill `project-build-workflow` §11.2 认领协议 + §11.9 看板同步 + §6.1/§6.2 拍板话术
3. 各任务的设计文档(见 board.json 里对应 task.docs 字段)

**每条决策的落地流程**:

```
# 认领(不 claim git commit 会被 pre-commit 硬闸门拦下)
node ~/.claude/dashboard/cli/index.cjs claim <任务id> --project game --branch <你的分支名>

# 按用户答案实施代码/设计
# ...(施工过程)

# 施工完成
node ~/.claude/dashboard/cli/index.cjs done <任务id> --project game --pr <PR号> --commit <sha>
node ~/.claude/dashboard/cli/index.cjs mark-landed <任务id> --did <决策id> --project game
```

**施工顺序建议**:
- 有明确依赖顺序的按依赖跑;无强依赖的按 wave/编号跑
- 每条决策独立分支、独立 PR、独立 auto-merge(见 skill §11.7)
- 中途冒出新的待拍板点(超范围/新决策) → `cli pending --json` 登记(必须给背景/利弊/推荐理由三件套,见 skill §6.2)

**关于"暂缓"类答案**(如 P15 "维持暂缓 + 编写职业晋升系统"这类不是"照代码改"而是"启动一个新方案"):
- 不是让你现在动代码,而是让你**新起一份设计方案文档**(放 `docs/plans/` 下,沿用项目编号规则)
- 按 skill §3 阶段①对账 + §4 阶段②建议 + §5 阶段③分解 走完,最后停下等用户拍板才能开工
- 联网搜索/对抗审查/对齐现有代码这些用户提到的都要做完

## 现在开始

别问"要不要开始"、别问"具体做什么"——上面就是全部任务书。请从第 1 条开始,先做该条的阶段①对账。
