# 排查笔记（滚动追加）

## 2026-09-06 · RGB 灯条跑完一轮会「跳回起点」（卡 UI-RGB-STRIP-SEAMLESS）

**症状（负责人原话）**：「这玩意播放一会儿后结束了，就直接从头开始……不是那种流畅的自然变换」「我想要的是现实中的那种无限循环，不要播到头了又从头开始，动画效果断了不爽」。

**现场代码**（改前 `web/src/styles/base.css`）：

```css
.glow { background-image: var(--spectrum); background-size: 220% 100%; animation: slide 3.4s linear infinite; }
@keyframes slide { to { background-position: -220% 0; } }
```

**根因**：位移量与背景图宽度不成整数倍。
`background-position` 的百分比不是「按图宽折算」，而是按 **（元素宽 − 图宽）** 折算：
图宽 = 元素宽 × 2.2 时，`-220%` 实际推出 `(1 − 2.2) × (−2.2) = 2.64` 倍元素宽 = **1.2 张图**。
一轮结束时画面比起点多偏了 0.2 张图，`infinite` 重启的瞬间硬切回起点 → 每 3.4 秒可见地跳一下。
色站本身是首尾同色的（`#4ad9c4 … #4ad9c4`），所以图与图接缝不可见，唯一的断点就是这个「跳」。

**铁证（冻帧对照，可复查）**：同一根灯条用 `animation-play-state: paused` + 负 `animation-delay`
分别冻在 `t=0` 与 `t=3.4s` 两帧，上下贴着比对（脚本 `seam-test.html`，跑在临时静态服务器上）：
- 旧写法 220%/-220%：硬边条纹底图肉眼可见台阶，光谱底图上下半明显色差 → 终点帧 ≠ 起点帧。
- 新写法 200%/-200%：条纹严丝合缝、光谱看不出分界 → 终点帧 = 起点帧。
反向验证成立（改法能让现象消失），故根因钉死。

**修法（治本）**：让一轮正好推走「整整一张背景图」——图宽 200%、位移 `-200%`
（`(1 − 2) × (−2) = 2` 倍元素宽 = 1 张图）；竖条同理 `100% 200%` / `0 -200%`。
色站首尾同色的约定写进 `base.css` 注释，自定义配色由 `utils/spectrum.ts` 的 `closeCycle()` 自动闭合，
防止后人加新配色时丢掉收尾色又把接缝带回来。

**根因三问**
1. 以前为什么会错：位移量按百分比折算后是 1.2 张图，不是整数张，一轮结束画面对不上。
2. 这个改动为什么让它变对：位移量与图宽相等，跑完一轮画面与起点逐像素相同，`infinite` 重启无跳变。
3. 为什么不会伤到别处：改动集中在 `.glow` / `.glow-rail > i` / `.progress > i` / `.glow-edge` 四条规则；
   全仓引用 `--spectrum` 的只剩审阅台 `Reader.vue`（其重复规则已删除，改为继承 base.css 同名规则），
   已用计算样式逐个回读确认（`background-size: 200% 100%`、`slide` / `slideY`、色站首尾同色）。

**回滚点**：本卡分支 `feat/rgb-strip-seamless` 的父提交 `7a95fcb`。
