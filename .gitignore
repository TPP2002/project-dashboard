node_modules/
dist/
*.log
*.bak-*
snapshots/
approvals/
status-drafts/
*.lock
*.exe
data/costUsageCache.json
.dashboard/
data/reader/
/registry.json

# 本仓自己也装了看板同步钩子(DASHBOARD-SELF-HOOK-MISSING),而 hooks-install 会顺手写下面两个
# 文件,它们里面焊的是【这台机器上发布副本的绝对路径】。本仓是公开仓,提交进去等于把用户名与
# 目录结构贴出去(会被 test/noLocalIdentifiers.test.cjs 当场拦下)。所以它们只留在本机:
# 面向所有 AI 工具的公开协议正本是 AGENTS.md,不是 CLAUDE.md。
/CLAUDE.md
/.claude/
