# 施工现场开发说明

## W1 施工记录

- 任务：AUD-FUN-WORKFLOOR，范围为 W1 规格 §1 的八项交付。
- 基线：`c94253a73473e5725e01b59abee69a1c52015794`。总契约的十一状态表是派单方提供的工作区更新，施工方不修改它。
- 设置沿用外观存档，折叠状态仅新增 `board-workfloor-ui`。
- 桥接复用现有 `derive.progress`，不修改看板、事件总线、状态定义或接口。
- 运行器统一管理一个三十帧循环；场景和特效不自行启动定时动画。
- 九个特效按样张的软边、噪声体积与受光层次实现，颜色字面量集中在色站。
- launch / mech 本轮仅挂测试台；不制作两个世界、白天调色板或声音播放。
- 验收 A1～A4、M1 由派单方执行，本工作区不运行这些验收命令。
- 回收只涉及本单允许路径的未提交改动；不执行任何 Git 写操作。

## 模块与接线

`AppearanceWorkfloor` 使用外观中心同一套控件和 `setAppearance`。五项设置保存在原有外观存档；归一化逐字段接收合法值，坏字段回默认。`resetAppearance` 也会恢复整组设置。

`WorkFloor` 放在看板页头与泳道之间，接收 `board`、`projectId`，并只读项目颜色设置和项目清单。未选择项目、无看板或世界为 off 时不占空间。折叠后为 36px 状态行；折叠存档只有一个布尔字段。主题跟随系统深浅变化，时钟模式每分钟及页面恢复时读取本机小时；W1 测试台仅保存并传递昼夜，不实现白天调色板。

`bridge.ts` 不依赖 Vue、Pinia 或 DOM。统计复用 `derive.progress`；十一状态按总契约映射。`SceneTask.order` 是分组内从零开始的排序位置，任务进度先夹到整数边界，收官再取至少 90。输入不会被改写，空看板和全作废看板都不视为完成。

`mapBoardEvent(event, projectId?, state?)` 支持单事件映射和显式项目过滤。当前事件总线没有 percent，因此容器从 store 的最新快照补齐，再把新状态与事件交给场景。这里直接读取 store 是为了避开父组件 props 尚未刷新的一小段同步时间。订阅只在挂载时建立、卸载时退订，项目切换不重新订阅，也不读取历史活动列表。

## 世界接口

公开入口 `createWorkfloor(svg, options)` 返回 `update(options)`、`handleEvent(event)`、`destroy()`。宿主必须是已挂载的唯一 SVG。`options` 包含 world、state、dayNight、detail、height、collapsed、sound 和 onError；可注入 `now` 供展示事件时间与完工合并使用。不要从场景直接访问 store。

场景模块导出 `createScene: SceneFactory`，返回 `SceneHandle`：

| 方法 | 责任 |
| --- | --- |
| `setState(state)` | 首帧直接摆好；后续以快照纠正事件临时效果；不得修改输入 |
| `handleEvent(event, animate)` | animate 为 false 时直接落位；done 仍须计数，不丢掉事件 |
| `tick(dt, elapsed)` | 单位均为毫秒；elapsed 只累计实际运行时间 |
| `setDayNight(dayNight)` | 接收解析后的 day / night |
| `setDetail(detail)` | 标准档粒子减半，去掉位移滤镜 |
| `setHeight(height)` | 设置 viewBox，中段裁切须保留主体 |
| `setReducedMotion(reduced)` | 减少动效时保持静态，事件直接落位 |
| `setSound(sound)` | 接收提示音总开关、音量、静音时段与联动开关，W1 不播放 |
| `destroy()` | 移除自己创建的节点、局部 clipPath 和特效实例 |

注册表保留 launch / mech 的动态 import，主入口不直接导入两个世界。W2 替换 `scenes/mech/index.ts`，W3 替换 `scenes/launch/index.ts`，分别导出相同 factory 即可。占位模块目前分别标注「MECH 场景待 W2」和「LAUNCH 场景待 W3」。`scenes/bench/index.ts` 的独立 `createScene` 保留给后续调试入口使用，本单不新增路由。

切项目或世界先淡出 300ms，再创建新场景并淡入 300ms。异步切换使用代次校验；过期的加载结果不会挂回宿主。切换期间收到的事件不排队重放，新场景按最新快照初始化。首次载入已经完成的项目不发 complete；之后 false → true 才派生 complete。按事件时间的整秒合并 done 动画，每个 done 仍送到场景；重复秒用 animate=false 标识，状态计数始终来自快照。

## 特效调用与调试

每个特效导出 `create(group, params)`，返回 `update(dt, params)` 和 `destroy()`。group 必须已挂在 SVG 中；位置和比例由外层 group 变换提供。通用参数是 detail、reducedMotion、enabled。所有实例只修改自己的子节点，不启动循环，不用位图或新增图标。

| 模块 | 参数和画法 |
| --- | --- |
| flame | intensity、length、onLight；三层贝塞尔火舌、冲击菱、湍流、喷口及地面柔光 |
| smoke | fireLight、moonLight；70 / 35 团固定池，实体底、噪声蒙版、错位明暗、暖底冷顶 |
| vent | density；28 / 14 组下沉气流，源头雾丝和地面冷雾 |
| beam | length、angle、sweep、surfaceX、surfaceClip；双层软楔形、三层灯室和裁切受光面 |
| lightning | period、length、silhouetteClip、onLight；三层通道与分叉、局部闪光、余辉及剪影受光 |
| frost | floor；32 / 16 片，重力阻力、自转、单次弹跳、落地渐隐 |
| sparks | intensity；48 / 24 条暖色拖尾，白热焊点与重力寿命曲线 |
| drone | target、mode（weld / inspect）、working；缓动飞行、悬停、螺旋桨、焊枪和检验束 |
| path | path（实际 SVGPathElement）、kind（crawler / fuel）、duration；沿切线行驶，端点缓停掉头 |

测试台中间九个展位持续演示；闪电周期 6000ms，右上只保留最近五条实时事件。状态摘要包含各分组计数和总进度，另列前三个施工任务的进度。标准与紧凑 viewBox 分别为 `0 0 1400 520` 和 `0 80 1400 360`，九个展位及摘要均在裁切范围内。

`fx/defs.ts` 每个宿主只注入一次共享 defs。滤镜、渐变和裁切 ID 使用独立 `wf-` 前缀；单个特效不移除共享 defs，由总入口最终清理。世界可复用 `ensureDefs`，但不能自行驱动它的时间。标准档选择没有 feDisplacementMap 的滤镜版本；滤镜仅挂局部特效，未挂整幅画面。所有颜色字面量在 `base.css` 色站，测试台 `palette.ts` 只保留语义引用。项目色优先本机覆盖，再取合法登记颜色，最后回落当前光谱首色。

## 循环、降级与验证边界

运行器只有一个 requestAnimationFrame 循环，通过时间累积限到每秒 30 帧。后台、离屏、折叠、切换以及减少动效使用独立暂停原因；恢复时清空上一帧基准和余数，卡顿也不补帧。减少动效不申请动画帧，只在状态或事件变化时直接画静态终态；切换淡入淡出也立即结束。粒子池在创建时分配，每帧不拼 innerHTML、不重建节点，未变化的属性不重复写入。

本单未运行 A1、A2、A3、A4 或 M1，全部交派单方验收。轻量检查记录：

- TypeScript `transpileModule` 和 Vue SFC 解析检查：24 个 TypeScript 文件、4 个 Vue 文件，第二次检查退出码 0，语法诊断 0。第一次发现烟运动表达式的负号与指数优先级错误，已加括号修正；这不是类型检查或打包证据。
- 尝试单次 `node --import tsx` 导入桥接样例时，tsx 初始化在 `uv_os_get_passwd` 报 `ENOMEM`，退出码 1，尚未运行到桥接代码。不修环境、不重试，不将此结果当成桥接逻辑失败或通过。新增单测按规定保留 tsx 子进程写法，派单环境需要提供可用的 tsx。
- 单测为真实 bridge / derive / schema 调用；仅用 Node 模块解析钩子补齐 Vite 的虚拟 schema，并从现有核心状态表取得 STATUS，不替换被测逻辑。覆盖十一状态、排序、进度边界、输入不变、空板/全作废/全部完成、全部事件映射及异项目过滤。
- 视觉质感、真实交互、事件探针、减少动效静止和切换不重放，以及后续世界的每帧性能，均没有以源码检查冒充通过。
- 收尾轻量检查：新增桥接测试的 `node --check` 退出码 0；workfloor 范围可读性检查为 23 个源文件、0 错误、0 警告；已有文件的本单 diff 空白检查退出码 0；10 个必需产物均存在。首次通过 PowerShell 管道传入 Node 的中文文档路径被编码成问号，产生一次错误缺失提示；随后直接按磁盘中文路径核对，确认文档存在，未改动或重建它。

施工期间观察到其他文件同时发生变化：`test/codexPanel.test.cjs`、`test/readerApi.test.cjs`、`test/server.test.cjs`、`test/serverRuntimeIdentity.test.cjs` 以及新增 `scripts/free-port.cjs`。这些不在本单改动清单内，施工方未写入或回滚它们；派单方回收时应分清来源。
