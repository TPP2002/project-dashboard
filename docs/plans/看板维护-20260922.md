# 看板维护 · 2026-09-22

## 范围与现场

- 用户交办八张卡，施工方为本 Codex 对话，不使用派单器，不设人为额度上限。
- 基线：`04b73854a0b2895c46757a830d72598b48d5bf7e`；分支：`codex/dash-maintenance-0922`；独立 worktree，保护主仓既有未提交文件。
- `QUOTA-SPLIT-SERVER` 已按用户指令经 CLI 作废；不删除既有实现或远端服务。
- 禁区：`board.json` 手改、另一张在施工的 `DASH-SCHED-CI-QUEUED-JOBS` 文件域、无关产品改造、共享调度器实现。
- CI 为 GitHub 云端 Windows runner；云 CI 不占本地机器，不进入局域网调度队列。

## 微型设计与验收清单

| 卡 | 现状与改动 | 精确验收 |
|---|---|---|
| UDQ-ENROLL-DASHBOARD / UDQ-ONBOARD-DASHBOARD | 本项目会在本机跑全量单测、类型检查和构建，适用接入。npm 正式入口通过共享 `sched.cjs` submit → wait → run；失败或中断 cancel；核数由许可控制。现阶段无 e2e/跑分脚本，不虚构已接入入口，提供显式作业包装入口供未来使用。 | 调度包装定向测试；最终实际挂号记录；全量单测、web 类型检查与构建从正式 npm 入口开跑。 |
| DASH-TEST-SUITE-CONCURRENT-INTERFERENCE | `findExistingInstance` 只匹配 service，端口扫描会碰到另一个注册表的服务；增加数据根/注册表归属，保持同数据实例升级接管。 | 两个重叠端口段、不同注册表的真实服务同时存活；现有同实例复用/升级测试。 |
| DASH-PRECHECK-DOCS-INDEX-AUTOREFRESH | precheck 在指定工位检测 docs:index 与 node_modules，满足才运行；失败只提醒且保持开工检查成功。 | 无 package、缺脚本、缺依赖、成功、子命令失败与路径含空格。 |
| DASH-COSTCACHE-WRITES-INTO-RELEASE | 增量缓存移至 DASHBOARD_HOME，创建父目录，写失败可见；聚合口径不变。 | 新目录写入、重启复用、发布代码目录无缓存、失败提醒；既有成本聚合测试。 |
| DASH-DONE-CARD-STALE-NEXT-MILESTONE | 默认 done 清 nextMilestone，collect 保留；状态机说明同步。 | progress → done 清字段、collect 保留、开销闸拒绝时不改字段。 |
| DASH-CPU-BUDGET-LEDGER-RETIRED | 机器卡已经读调度心跳；删除退休账本及预留双写、失效重试入口。保留调度队列的预留和机器真实状态，不把不可读显示为零。 | 调度 API/读取/预留测试；真实页面无旧账本提示，机器数据仍可见。 |
| DASH-SCHED-CI-SLOWDOWN-CALIBRATE | 台账按 attempt 许可 ciAware 分桶，带标至少 5 个且普通中位数有效时实测比值；不足回退 1.5 并显示估。 | 4/5 样本边界、分桶不混、重跑许可、缺普通样本/零时长、页面估计标识。 |

## 验证与交付

开发期仅定向测试，最终候选统一全量；本机重活全部经排队客户端，云 CI 核对精确 head SHA。环境不支持离机执行本仓时走本机授予许可，不调用 rogue 专用集群作业冒充本仓验证。客户端不可用明确失败，不悄悄裸跑。

收官次序：原子提交 → push/PR → 精确候选 CI 通过 → 合主干 → 由发布副本 CLI release → 重启并核对 health/页面版本 → cost 与 done 回写。回滚以各卡原子提交为单位，不改写共享历史。

## 调查与证据

- 开工已核对调度正本 §5.9：共享客户端已上线；旧账本退休；机器配额与 CI 余量由统一调度控制。
- 源码证据：`server/server.cjs` 的 `findExistingInstance` 仅检查 `h.service === SERVICE`；`core/cpuBudget.cjs` 仍读取/写入租约；`package.json` 的全量入口为裸 `node --test`。
- 工具探测中两次 Windows glob 被 rg 当字面路径，已改为目录加 `-g` 搜索；未改动任何产品文件。
- 测试、发布与页面验收：【待补】，未运行前不作完成声明。
